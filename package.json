{
  "name": "action-aws-cli",
  "version": "0.0.1",
  "private": true,
  "description": "Installs AWS cli",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js --license LICENSE -o v1",
    "run": "node v1/index.js",
    "test": "jest --coverage",
    "publish": "branch=$(git rev-parse --symbolic-full-name --abbrev-ref HEAD); if [[ $branch == \"master\" ]]; then npm run-script \"change-branch\"; else npm run-script \"create-publish\"; fi;",
    "change-branch": "echo \"Should not publish on master\" && exit 1",
    "create-publish": "npm i && npm run build && git rm -rq --ignore-unmatch ./node_modules && rm -rf ./node_modules && npm i --production && git add . && echo \"Ready to commit\" && exit 0"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:ohthepain/action-install-aws-cli.git"
  },
  "keywords": [
    "actions",
    "node",
    "setup"
  ],
  "author": "paulwilkinson based on a fork chrislennon's repo",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.0.0",
    "@actions/exec": "^1.0.0",
    "@actions/github": "^1.0.0",
    "@actions/io": "^1.0.0",
    "@actions/tool-cache": "^2.0.1",
    "firstline": "^2.0.2",
    "semver": "^6.1.1"
  },
  "devDependencies": {
    "@types/jest": ">24.0.13",
    "@types/nock": ">10.0.3",
    "@types/node": ">20.0.0",
    "@types/semver": ">6.0.0",
    "@typescript-eslint/eslint-plugin": ">1.13.0",
    "@typescript-eslint/parser": ">1.13.0",
    "eslint": ">5.16.0",
    "jest": "^29.7.0",
    "jest-circus": ">24.7.1",
    "nock": ">10.0.6",
    "typescript": ">3.5.1"
  }
}
